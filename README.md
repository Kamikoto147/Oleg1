# Discord-like Messenger

Полнофункциональный мессенджер в стиле Discord с современной архитектурой, высокой производительностью и расширенными возможностями.

## 🚀 Возможности

### Основной функционал
- ✅ **Аутентификация**: Регистрация, вход, выход с безопасным хешированием паролей
- ✅ **Пользователи**: Профили, статусы, поиск, система друзей
- ✅ **Серверы (Гильдии)**: Создание, управление, приглашения, роли и права
- ✅ **Каналы**: Текстовые каналы с категориями, настройки, права доступа
- ✅ **Сообщения**: Отправка, редактирование, удаление, реакции, закрепление
- ✅ **Файлы**: Загрузка изображений, документов, drag & drop
- ✅ **Real-time**: Мгновенные обновления через WebSockets

### Расширенные функции
- ✅ **Поиск**: По сообщениям и пользователям с фильтрацией
- ✅ **Эмодзи**: Кастомные эмодзи для серверов
- ✅ **Стикеры**: Загрузка и использование стикеров
- ✅ **Опросы**: Создание опросов с голосованием и истечением
- ✅ **Анимации**: Плавные переходы и skeleton-загрузка
- ✅ **Производительность**: Пагинация, кэширование, виртуализация
- ✅ **Безопасность**: CSRF защита, rate limiting, валидация
- ✅ **Тестирование**: Полное покрытие unit и интеграционными тестами

## 🛠 Технологии

### Backend
- **Python 3.8+** - Основной язык
- **Flask** - Web фреймворк
- **Flask-SocketIO** - Real-time коммуникация
- **SQLAlchemy** - ORM для работы с БД
- **Werkzeug** - Безопасность и утилиты
- **SQLite** - База данных (разработка)

### Frontend
- **HTML5** - Разметка
- **CSS3** - Стили с поддержкой темной темы
- **JavaScript ES6+** - Интерактивность
- **Socket.IO** - Real-time клиент
- **Drag & Drop API** - Загрузка файлов

### Инфраструктура
- **Кэширование** - In-memory кэш с TTL
- **Тестирование** - Unit, интеграционные, E2E тесты
- **Безопасность** - CSRF, rate limiting, валидация

## 📦 Установка и запуск

### Быстрый старт

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd discord-messenger
```

2. **Установка зависимостей:**
```bash
pip install -r requirements.txt
```

3. **Инициализация базы данных:**
```bash
python backend/init_permissions.py
```

4. **Запуск сервера:**
```bash
python backend/app.py
```

5. **Открытие в браузере:**
```
http://localhost:5000
```

### Детальная установка

#### Системные требования
- Python 3.8 или выше
- pip (менеджер пакетов Python)
- Современный веб-браузер
- 512 MB RAM (минимум)
- 100 MB свободного места

#### Установка зависимостей
```bash
# Основные зависимости
pip install -r requirements.txt

# Для разработки и тестирования
pip install -r tests/requirements.txt
```

#### Настройка окружения
```bash
# Создание виртуального окружения (рекомендуется)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt
```

## 🏗 Архитектура

### Структура проекта
```
discord-messenger/
├── backend/                    # Backend код
│   ├── app.py                 # Основное Flask приложение
│   ├── models.py              # SQLAlchemy модели
│   ├── auth.py                # Аутентификация и авторизация
│   ├── users.py               # Управление пользователями
│   ├── guilds.py              # Управление гильдиями и каналами
│   ├── messages.py            # Управление сообщениями
│   ├── files.py               # Загрузка и обработка файлов
│   ├── utils.py               # Утилиты и хелперы
│   ├── cache.py               # Система кэширования
│   ├── emojis_stickers_polls.py  # Эмодзи, стикеры, опросы
│   └── init_permissions.py    # Инициализация прав
├── templates/                 # HTML шаблоны
│   └── index.html            # Главная страница
├── static/                   # Статические файлы
│   ├── style.css            # Стили
│   └── script.js            # JavaScript логика
├── uploads/                  # Загруженные файлы
├── tests/                    # Автоматические тесты
│   ├── test_auth.py         # Тесты аутентификации
│   ├── test_messages.py     # Тесты сообщений
│   ├── test_guilds.py       # Тесты гильдий
│   ├── test_integration.py  # Интеграционные тесты
│   ├── test_frontend.py     # Frontend тесты
│   └── run_tests.py         # Запуск всех тестов
├── requirements.txt          # Python зависимости
└── README.md                # Документация
```

### Архитектурные принципы

#### Backend
- **Модульность**: Разделение на логические модули
- **ORM**: Использование SQLAlchemy для работы с БД
- **Безопасность**: CSRF защита, rate limiting, валидация
- **Производительность**: Кэширование, пагинация, оптимизация
- **Тестируемость**: Полное покрытие тестами

#### Frontend
- **Реактивность**: Real-time обновления через WebSockets
- **UX**: Плавные анимации, skeleton-загрузка
- **Производительность**: Виртуализация, lazy loading
- **Доступность**: Поддержка клавиатурных сокращений

## 🔌 API Документация

### Аутентификация

#### Регистрация пользователя
```http
POST /api/register
Content-Type: application/json

{
  "username": "testuser",
  "password": "securepassword",
  "email": "test@example.com"
}
```

**Ответ:**
```json
{
  "id": 1,
  "username": "testuser",
  "email": "test@example.com",
  "created_at": "2024-01-01T00:00:00Z"
}
```

#### Вход в систему
```http
POST /api/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "securepassword"
}
```

**Ответ:**
```json
{
  "id": 1,
  "username": "testuser",
  "email": "test@example.com"
}
```

### Пользователи

#### Поиск пользователей
```http
GET /api/search/users?q=alice
```

**Ответ:**
```json
{
  "results": [
    {
      "username": "alice",
      "avatar": "avatar_url"
    }
  ],
  "count": 1
}
```

#### Система друзей
```http
POST /api/friends/request
Content-Type: application/json
X-CSRF-Token: <token>

{
  "to": "alice"
}
```

### Гильдии

#### Создание гильдии
```http
POST /api/guilds
Content-Type: application/json
X-CSRF-Token: <token>

{
  "name": "My Server"
}
```

#### Список каналов гильдии
```http
GET /api/guilds/1/channels
```

**Ответ:**
```json
[
  {
    "id": 1,
    "name": "general",
    "type": "text",
    "position": 0
  }
]
```

### Сообщения

#### Получение сообщений с пагинацией
```http
GET /api/channels/1/messages?page=1&limit=50
```

**Ответ:**
```json
{
  "messages": [
    {
      "id": 1,
      "content": "Hello, world!",
      "username": "alice",
      "timestamp": "2024-01-01T00:00:00Z",
      "pinned": false,
      "files": []
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 50,
    "total": 100,
    "pages": 2
  }
}
```

#### Отправка сообщения
```http
POST /api/channels/1/messages
Content-Type: application/json
X-CSRF-Token: <token>

{
  "content": "Hello, everyone!"
}
```

### Файлы

#### Загрузка файла
```http
POST /api/upload
Content-Type: multipart/form-data
X-CSRF-Token: <token>

file: <file_data>
```

### Эмодзи и стикеры

#### Создание кастомного эмодзи
```http
POST /api/guilds/1/emojis
Content-Type: multipart/form-data
X-CSRF-Token: <token>

name: "custom_emoji"
file: <emoji_file>
```

#### Список стикеров гильдии
```http
GET /api/guilds/1/stickers
```

### Опросы

#### Создание опроса
```http
POST /api/messages/1/poll
Content-Type: application/json
X-CSRF-Token: <token>

{
  "question": "What is your favorite color?",
  "options": ["Red", "Blue", "Green"],
  "allow_multiple": false,
  "expires_hours": 24
}
```

#### Голосование в опросе
```http
POST /api/polls/1/vote
Content-Type: application/json
X-CSRF-Token: <token>

{
  "option_id": "1"
}
```

## 🔌 WebSocket API

### События клиента

#### Присоединение к комнате
```javascript
socket.emit('join_room', { room: 'g:1:c:2' });
```

#### Отправка сообщения
```javascript
socket.emit('send_message', {
  room: 'g:1:c:2',
  content: 'Hello!',
  file: fileData  // опционально
});
```

#### Индикатор печати
```javascript
socket.emit('typing', { room: 'g:1:c:2' });
```

#### Реакции
```javascript
socket.emit('add_reaction', {
  room: 'g:1:c:2',
  message_id: 123,
  emoji: '👍'
});
```

### События сервера

#### Новое сообщение
```javascript
socket.on('new_message', (data) => {
  console.log('New message:', data);
});
```

#### Пользователь печатает
```javascript
socket.on('user_typing', (data) => {
  console.log('User typing:', data.username);
});
```

#### Реакция добавлена
```javascript
socket.on('reaction_added', (data) => {
  console.log('Reaction added:', data);
});
```

## 🧪 Тестирование

### Запуск всех тестов
```bash
cd tests
python run_tests.py
```

### Типы тестов

#### Unit тесты
- Тестируют отдельные функции и методы
- Быстрые и изолированные
- Не требуют внешних зависимостей

```bash
python -m unittest test_auth.py
python -m unittest test_messages.py
python -m unittest test_guilds.py
```

#### Интеграционные тесты
- Тестируют взаимодействие между компонентами
- Используют реальную базу данных
- Проверяют API эндпоинты

```bash
python -m unittest test_integration.py
```

#### Frontend тесты
- Тестируют пользовательский интерфейс
- Используют Selenium WebDriver
- Проверяют взаимодействие пользователя

```bash
python -m unittest test_frontend.py
```

### Покрытие кода
```bash
pip install pytest-cov
pytest --cov=backend tests/
```

## 🔒 Безопасность

### Реализованные меры

#### CSRF защита
- Токены CSRF для всех POST запросов
- Валидация токенов на сервере
- Автоматическое обновление токенов

#### Rate Limiting
- Ограничение запросов по IP
- Ограничение WebSocket событий
- Настраиваемые лимиты для разных операций

#### Валидация данных
- Проверка типов и форматов
- Санитизация пользовательского ввода
- Ограничения на размер файлов

#### Аутентификация
- Хеширование паролей с Werkzeug
- Сессии с безопасными токенами
- Защита от брутфорса

### Рекомендации по безопасности

1. **Используйте HTTPS** в продакшене
2. **Настройте CORS** для вашего домена
3. **Ограничьте размер файлов** и типы
4. **Регулярно обновляйте** зависимости
5. **Мониторьте логи** на подозрительную активность

## ⚡ Производительность

### Оптимизации

#### Backend
- **Кэширование**: In-memory кэш с TTL для часто запрашиваемых данных
- **Пагинация**: Ограничение количества сообщений на странице
- **Индексы БД**: Оптимизированные запросы к базе данных
- **Rate Limiting**: Защита от перегрузки сервера

#### Frontend
- **Виртуализация**: Рендеринг только видимых элементов
- **Lazy Loading**: Загрузка данных по требованию
- **Debouncing**: Оптимизация поиска и ввода
- **Skeleton Loading**: Улучшение воспринимаемой производительности

### Мониторинг

#### Метрики производительности
- Время ответа API
- Использование памяти
- Количество активных соединений
- Размер кэша

#### Рекомендации
1. **Мониторьте производительность** в продакшене
2. **Используйте профилировщики** для оптимизации
3. **Настройте логирование** для отладки
4. **Планируйте масштабирование** заранее

## 🚀 Развертывание

### Локальная разработка
```bash
# Клонирование и установка
git clone <repository-url>
cd discord-messenger
pip install -r requirements.txt

# Запуск в режиме разработки
python backend/app.py
```

### Продакшен

#### Docker (рекомендуется)
```dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
EXPOSE 5000

CMD ["python", "backend/app.py"]
```

#### Системные требования
- **CPU**: 1-2 ядра
- **RAM**: 512 MB - 2 GB
- **Диск**: 1-10 GB (зависит от файлов)
- **Сеть**: Стабильное подключение

#### Переменные окружения
```bash
export FLASK_ENV=production
export SECRET_KEY=your-secret-key
export DATABASE_URL=postgresql://user:pass@host/db
```

## 🤝 Разработка

### Добавление новых функций

1. **Создайте модель** в `backend/models.py`
2. **Добавьте бизнес-логику** в соответствующий модуль
3. **Создайте API эндпоинты** в `backend/app.py`
4. **Обновите frontend** в `static/script.js`
5. **Добавьте тесты** в `tests/`
6. **Обновите документацию**

### Структура кода

#### Backend модули
- `auth.py` - Аутентификация и авторизация
- `users.py` - Управление пользователями
- `guilds.py` - Гильдии и каналы
- `messages.py` - Сообщения и реакции
- `files.py` - Загрузка файлов
- `cache.py` - Кэширование
- `utils.py` - Утилиты

#### Frontend компоненты
- Основной класс `MessengerApp`
- Модули для разных функций
- Обработчики событий
- Утилиты для UI

### Соглашения по коду

#### Python
- PEP 8 стиль кодирования
- Типизация с помощью type hints
- Документация функций
- Обработка ошибок

#### JavaScript
- ES6+ синтаксис
- Модульная структура
- Обработка ошибок
- Комментарии для сложной логики

## 📝 Лицензия

MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🙏 Благодарности

- Discord за вдохновение в дизайне
- Flask и SQLAlchemy за отличные инструменты
- Сообщество Python за поддержку

## 📞 Поддержка

Если у вас есть вопросы или проблемы:

1. Проверьте [Issues](https://github.com/your-repo/issues)
2. Создайте новый Issue с описанием проблемы
3. Приложите логи и шаги для воспроизведения

---

**Сделано с ❤️ для сообщества разработчиков**